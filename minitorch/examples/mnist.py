from __future__ import annotations


def main() -> None:
    print("MNIST support is planned next. The core engine, layers, and optimizers are in place.")
    print("Once we add a dataset loader and a larger example, this script can become the CPU digit classifier entrypoint.")


if __name__ == "__main__":
    main()
